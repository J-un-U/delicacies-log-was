spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: CLiENT_ID
  mail:
    password: NEED_TO_INPUT
custom:
  security:
    oauth2:
      client:
        registration:
          kakao:
            devUser:
              oauthId: oauthId
              nickname: nickname